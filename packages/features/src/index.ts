/**
 * Features live here: a Zustand store per slice of client state, an operation
 * function per mutation, and a command factory that exposes both to the agent.
 *
 * Nothing in this package may import React, React Native, Expo, or React
 * Navigation. `@apollo/client/core` is allowed, `@apollo/client` is not.
 * dependency-cruiser enforces both.
 */
export {};
